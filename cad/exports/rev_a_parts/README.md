# Rev A Part STL Exports

Individual print-review STLs should be exported here after each part renders successfully in OpenSCAD.

This folder is intentionally separate from `prototype1_gimbal_baseline.stl` because the baseline STL is a combined layout model. The files here should be separated part exports suitable for slicer fit checks.
